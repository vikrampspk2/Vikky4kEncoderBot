# NEVER commit real secrets.
BOT_TOKEN=
# Exactly TWO numeric Telegram owner IDs, comma-separated. Never use @username.
OWNER_IDS=123456789,987654321

# Private payment configuration. Keep these out of GitHub source.
FAMPAY_UPI=
VIKKY_QR_PATH=config/payment/fampay_qr.png

VIKKY_MAX_INPUT_GB=60
VIKKY_LIVE_INTERVAL=5
CONTACT_DELETE_HOURS=24
# Optional real worker command. It MUST create the output path as MKV.
# VIKKY_WORKER_CMD=python workers/ai_worker.py "{input}" "{output}" "{mode}" "{profile}"
